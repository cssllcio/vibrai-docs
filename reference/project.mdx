---
title: "Project"
description: "Initialize, load, save, and inspect Vibrai project files."
---

{/* GENERATED — do not edit by hand. Regenerate with `npm run generate:reference`. */}

Initialize, load, save, and inspect Vibrai project files.

## Command line

### `project init`

Initialize a new .vibrai project file

```bash
vibrai project init --name <NAME> --project <PATH> --demo --genre <NAME> --preset <PRESET> --number-tracks
```

<ParamField path="--name">
  Project name (default: basename of --project, or "Untitled")
</ParamField>
<ParamField path="--project">
  Path to .vibrai file (default: Untitled.vibrai in CWD; auto-uniquified to Untitled-2.vibrai, etc. if it already exists)
</ParamField>
<ParamField path="--demo">
  Write opinionated demo content (3 sections, 2 parts, exercises variation + scale overrides + naming) instead of the boilerplate
</ParamField>
<ParamField path="--genre">
  Seed from a genre library by id or display name (run 'vibrai genre list')
</ParamField>
<ParamField path="--preset">
  Preset id or display name within --genre (default: first preset alphabetically)
</ParamField>
<ParamField path="--number-tracks">
  Persist Ableton '#' track auto-numbering into the new project.
</ParamField>

### `project load`

Load and display a project file

```bash
vibrai project load --project <PATH>
```

<ParamField path="--project">
  Path to .vibrai file (default: Untitled.vibrai in CWD)
</ParamField>

### `project save`

Save the current project state

```bash
vibrai project save --project <PATH>
```

<ParamField path="--project">
  Path to .vibrai file (default: Untitled.vibrai in CWD)
</ParamField>

### `project info`

Show project file info

```bash
vibrai project info --project <PATH>
```

<ParamField path="--project">
  Path to .vibrai file (default: Untitled.vibrai in CWD)
</ParamField>

### `project config`

Edit persisted project preferences (e.g. --number-tracks)

```bash
vibrai project config --project <PATH> --number-tracks <BOOL>
```

<ParamField path="--project">
  Path to .vibrai file (default: Untitled.vibrai in CWD)
</ParamField>
<ParamField path="--number-tracks">
  Persist Ableton '#' track auto-numbering: true or false.
</ParamField>

## MCP tools

### `init_project`

Create a new .vibrai YAML file at the given absolute path. Without `demo`, writes a minimal 2-section / 1-part boilerplate. With `demo: true`, writes an opinionated 3-section / 2-part starter that exercises variation, per-section scale overrides, naming, and both generator types. Track IDs are probed from the bridge (first N MIDI tracks) and each probed binding records the track's current name (part `track_name`) so `generate` reuses those tracks; if the bridge is unreachable, falls back to track 1 (and 2 for demo) with no binding. Set number_tracks=true to persist Ableton '#' track auto-numbering into the new project's Defaults.

```json
{
  "tool": "init_project",
  "arguments": {
    "path": "<String>",
    "name": "<String>",
    "demo": "<Boolean>",
    "number_tracks": "<Boolean>"
  }
}
```

<ParamField path="path" type="String" required>
  Absolute filesystem path for the new .vibrai file.
</ParamField>
<ParamField path="name" type="String">
  Project display name. Defaults to the basename of `path` (e.g. "my-song" for `…/my-song.vibrai`).
</ParamField>
<ParamField path="demo" type="Boolean">
  If true, write the demo content instead of the boilerplate.
</ParamField>
<ParamField path="number_tracks" type="Boolean">
  Persist Ableton '#' track auto-numbering into the new project.
</ParamField>

### `load_project`

Load a .vibrai YAML file from the given absolute path. Returns the VibraiProject. Returns null if the file is missing.

```json
{
  "tool": "load_project",
  "arguments": {
    "path": "<String>"
  }
}
```

<ParamField path="path" type="String" required>
  Absolute filesystem path to the .vibrai file.
</ParamField>

### `save_project`

Save a VibraiProject (same shape as load_project's return value) to the given absolute path, overwriting any existing file. Validates the project first; rejects invalid input (e.g. section modifier &lt;= 0) without writing the file.

```json
{
  "tool": "save_project",
  "arguments": {
    "path": "<String>",
    "project": "<VibraiProject>"
  }
}
```

<ParamField path="path" type="String" required>
  Absolute filesystem path to write to.
</ParamField>
<ParamField path="project" type="VibraiProject" required>
  Project document, same shape as load_project's return value.
</ParamField>

### `project_info`

Return a summary of the project (counts of sections, parts, templates) at the given path.

```json
{
  "tool": "project_info",
  "arguments": {
    "path": "<String>"
  }
}
```

<ParamField path="path" type="String" required>
  Absolute filesystem path to the .vibrai file.
</ParamField>

### `configure_project`

Edit project-level preferences in an existing .vibrai file. Currently: number_tracks (persist Ableton '#' track auto-numbering). Loads, updates project.defaults, and saves. Errors if the file is missing.

```json
{
  "tool": "configure_project",
  "arguments": {
    "path": "<String>",
    "number_tracks": "<Boolean>"
  }
}
```

<ParamField path="path" type="String" required>
  Absolute filesystem path to the .vibrai file.
</ParamField>
<ParamField path="number_tracks" type="Boolean" required>
  Persist Ableton '#' track auto-numbering (true) or disable it (false).
</ParamField>
