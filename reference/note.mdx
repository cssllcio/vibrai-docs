---
title: "Notes"
description: "Read and write MIDI notes in clips."
---

{/* GENERATED — do not edit by hand. Regenerate with `npm run generate:reference`. */}

Read and write MIDI notes in clips.

## Command line

### `note list`

List notes in a clip

```bash
vibrai note list <track> <scene>
```

<ParamField path="track" required>
  Track handle (trk_42) OR 1-based number (1, 2, …) OR ordinal (first, second, …)
</ParamField>
<ParamField path="scene" required>
  Scene handle (scn_7) OR 1-based number (1, 2, …) OR ordinal (first, second, …)
</ParamField>

### `note set`

Replace all notes in a clip

```bash
vibrai note set <track> <scene> --json-data <JSON> --file <PATH>
```

<ParamField path="track" required>
  Track handle (trk_42) OR 1-based number (1, 2, …) OR ordinal (first, second, …)
</ParamField>
<ParamField path="scene" required>
  Scene handle (scn_7) OR 1-based number (1, 2, …) OR ordinal (first, second, …)
</ParamField>
<ParamField path="--json-data">
  Notes as a JSON array of note objects. Each note: &#123;"pitch": 0-127 or "C4", "start_time": beats, "duration": beats (>0), "velocity": 0-127 or "mf"}. Optional: pressure, slide, pitch_bend. Example: --json-data '[&#123;"pitch":60,"start_time":0,"duration":0.25,"velocity":100}]'
</ParamField>
<ParamField path="--file">
  Path to a JSON file containing the same note array as --json-data.
</ParamField>

### `note add`

Add notes to a clip

```bash
vibrai note add <track> <scene> --json-data <JSON> --file <PATH>
```

<ParamField path="track" required>
  Track handle (trk_42) OR 1-based number (1, 2, …) OR ordinal (first, second, …)
</ParamField>
<ParamField path="scene" required>
  Scene handle (scn_7) OR 1-based number (1, 2, …) OR ordinal (first, second, …)
</ParamField>
<ParamField path="--json-data">
  Notes as a JSON array of note objects. Each note: &#123;"pitch": 0-127 or "C4", "start_time": beats, "duration": beats (>0), "velocity": 0-127 or "mf"}. Optional: pressure, slide, pitch_bend. Example: --json-data '[&#123;"pitch":60,"start_time":0,"duration":0.25,"velocity":100}]'
</ParamField>
<ParamField path="--file">
  Path to a JSON file containing the same note array as --json-data.
</ParamField>

### `note remap`

Move every note at one pitch to another, server-side (session/arrangement/both)

```bash
vibrai note remap <track> --from <PITCH> --to <PITCH> --scope <SCOPE> --scene <N>
```

<ParamField path="track" required>
  Track handle (trk_42) OR 1-based number (1, 2, …) OR ordinal (first, second, …)
</ParamField>
<ParamField path="--from">
  Source MIDI pitch 0-127 (only notes at this pitch move)
</ParamField>
<ParamField path="--to">
  Target MIDI pitch 0-127
</ParamField>
<ParamField path="--scope">
  session, arrangement, or both (default both)
</ParamField>
<ParamField path="--scene">
  Scene handle (scn_7) OR 1-based number OR ordinal, to limit to one session clip; requires --scope session
</ParamField>

## MCP tools

### `list_notes`

List all notes in a clip.

```json
{
  "tool": "list_notes",
  "arguments": {
    "track": "<String>",
    "scene": "<String>"
  }
}
```

<ParamField path="track" type="String" required>
  Track handle from list_tracks, e.g. "trk_42". Copy verbatim.
</ParamField>
<ParamField path="scene" type="String" required>
  Scene handle from list_scenes, e.g. "scn_7". Copy verbatim.
</ParamField>

### `write_notes`

Replace all notes in a clip with the given list. Each note is an object with exact field names — 'start_time', not 'start'. Returns how many notes were requested vs. actually written.

```json
{
  "tool": "write_notes",
  "arguments": {
    "track": "<String>",
    "scene": "<String>",
    "notes": "<NoteEvent[]>"
  }
}
```

<ParamField path="track" type="String" required>
  Track handle from list_tracks, e.g. "trk_42". Copy verbatim.
</ParamField>
<ParamField path="scene" type="String" required>
  Scene handle from list_scenes, e.g. "scn_7". Copy verbatim.
</ParamField>
<ParamField path="notes" type="NoteEvent[]" required>
  Notes to write (replaces all existing). Each note object: pitch (integer 0-127 or note name e.g. "C4", required), start_time (number, beats from clip start, required), duration (number, beats, must be > 0, required), velocity (integer 0-127 or dynamic e.g. "mf", required), pressure/slide/pitch_bend (integer or null, optional, MPE). Field names are exact — "start_time", not "start".
</ParamField>

### `add_notes`

Append notes to a clip without removing existing notes. Each note is an object with exact field names — 'start_time', not 'start'. Returns how many notes were requested vs. actually written.

```json
{
  "tool": "add_notes",
  "arguments": {
    "track": "<String>",
    "scene": "<String>",
    "notes": "<NoteEvent[]>"
  }
}
```

<ParamField path="track" type="String" required>
  Track handle from list_tracks, e.g. "trk_42". Copy verbatim.
</ParamField>
<ParamField path="scene" type="String" required>
  Scene handle from list_scenes, e.g. "scn_7". Copy verbatim.
</ParamField>
<ParamField path="notes" type="NoteEvent[]" required>
  Notes to append. Each note object: pitch (integer 0-127 or note name e.g. "C4", required), start_time (number, beats from clip start, required), duration (number, beats, must be > 0, required), velocity (integer 0-127 or dynamic e.g. "mf", required), pressure/slide/pitch_bend (integer or null, optional, MPE). Field names are exact — "start_time", not "start".
</ParamField>

### `remap_pitch`

Move every note at from_pitch to to_pitch on one track, server-side — no note round-tripping, all other note fields preserved. Scope 'session', 'arrangement', or 'both' (default both); scene (session scope only) narrows to one session clip. Reports clips_touched/notes_changed. The drum-pad retargeting primitive; see retarget_drums for the automatic kit-swap flow.

```json
{
  "tool": "remap_pitch",
  "arguments": {
    "track": "<String>",
    "from_pitch": "<Int32>",
    "to_pitch": "<Int32>",
    "scope": "<String>",
    "scene": "<String>"
  }
}
```

<ParamField path="track" type="String" required>
  Track handle from list_tracks, e.g. "trk_42". Copy verbatim.
</ParamField>
<ParamField path="from_pitch" type="Int32" required>
  Source MIDI pitch 0-127 (only notes at this pitch move)
</ParamField>
<ParamField path="to_pitch" type="Int32" required>
  Target MIDI pitch 0-127
</ParamField>
<ParamField path="scope" type="String">
  'session', 'arrangement', or 'both' (default)
</ParamField>
<ParamField path="scene" type="String">
  Scene handle from list_scenes, e.g. "scn_7". Optional — limits the remap to one session clip; requires scope='session'. Omit (null) to leave the remap unscoped to a single scene.
</ParamField>
